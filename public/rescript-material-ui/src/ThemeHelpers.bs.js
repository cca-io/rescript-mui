// Generated by ReScript, PLEASE EDIT WITH CARE

import * as $$Array from "rescript/lib/es6/array.js";
import * as Curry from "rescript/lib/es6/curry.js";
import * as ReactDOMStyle from "@rescript/react/src/ReactDOMStyle.bs.js";

function addBreakpoint(sourceStyle, theme, breakpoint, style) {
  var breakpoint$1 = typeof breakpoint === "object" ? breakpoint.VAL : (
      breakpoint === "MD" ? "md" : (
          breakpoint === "SM" ? "sm" : (
              breakpoint === "XL" ? "xl" : (
                  breakpoint === "XS" ? "xs" : "lg"
                )
            )
        )
    );
  var breakpointSource = Curry._1(theme.breakpoints.up, breakpoint$1);
  return ReactDOMStyle.unsafeAddProp(sourceStyle, breakpointSource, style);
}

function checkAffect(affect, value) {
  if (affect) {
    return [value];
  } else {
    return [];
  }
}

function transitionCreate(theme, affectWidthOpt, affectMarginOpt, easing, duration, param) {
  var affectWidth = affectWidthOpt !== undefined ? affectWidthOpt : false;
  var affectMargin = affectMarginOpt !== undefined ? affectMarginOpt : false;
  var transitions = theme.transitions;
  var affects = $$Array.concat({
        hd: checkAffect(affectWidth, "width"),
        tl: {
          hd: checkAffect(affectMargin, "margin"),
          tl: /* [] */0
        }
      });
  var args = {
    easing: easing,
    duration: duration
  };
  return transitions.create(affects, args);
}

export {
  addBreakpoint ,
  checkAffect ,
  transitionCreate ,
  
}
/* No side effect */
