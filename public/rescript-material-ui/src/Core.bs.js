// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Curry from "rescript/lib/es6/curry.js";

var UNSAFE_INTERNAL = {};

function getAsString(theme, breakpoint) {
  var bpGet = breakpoint.NAME === "up" ? (function (v) {
        return v.up;
      }) : (function (v) {
        return v.down;
      });
  var tmp;
  if (breakpoint.NAME === "up") {
    var match = breakpoint.VAL;
    tmp = typeof match === "object" ? match.VAL : (
        match === "md" ? "md" : (
            match === "sm" ? "sm" : (
                match === "xl" ? "xl" : (
                    match === "xs" ? "xs" : "lg"
                  )
              )
          )
      );
  } else {
    var match$1 = breakpoint.VAL;
    tmp = typeof match$1 === "object" ? match$1.VAL : (
        match$1 === "md" ? "md" : (
            match$1 === "sm" ? "sm" : (
                match$1 === "xl" ? "xl" : (
                    match$1 === "xs" ? "xs" : "lg"
                  )
              )
          )
      );
  }
  return Curry._2(bpGet, theme.breakpoints, tmp);
}

var get = getAsString;

var Breakpoint = {
  UNSAFE_INTERNAL: UNSAFE_INTERNAL,
  getAsString: getAsString,
  get: get
};

var ServerStyleSheets = {};

export {
  Breakpoint ,
  ServerStyleSheets ,
  
}
/* No side effect */
